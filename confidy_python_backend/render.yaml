services:
  - type: web
    name: confidy-nlp-api
    env: python
    plan: free
    rootDir: confidy_python_backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
